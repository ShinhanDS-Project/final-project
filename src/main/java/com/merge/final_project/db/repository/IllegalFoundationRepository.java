package com.merge.final_project.db.repository;

import com.merge.final_project.db.entity.IllegalFoundation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IllegalFoundationRepository extends JpaRepository<IllegalFoundation, Integer> {
}
